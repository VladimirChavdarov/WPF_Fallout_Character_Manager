<UserControl x:Class="WPF_Fallout_Character_Manager.Controls.TextBoxWithLabelAndCheckboxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Fallout_Character_Manager.Controls"
             xmlns:conv="clr-namespace:WPF_Fallout_Character_Manager.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <conv:SPECIALToShortConverter x:Key="SPECIALToShortConverter"/>
        <conv:EqualityToBoolConverter x:Key="EqualityToBoolConverter"/>
    </UserControl.Resources>

    <Grid>
        <StackPanel
            FlowDirection="LeftToRight"
            Orientation="{Binding Orientation, RelativeSource={RelativeSource AncestorType=UserControl}}"
            HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}">
            
            <CheckBox
                Style="{StaticResource FalloutSkillCheckbox}"
                VerticalAlignment="Center"
                FontSize="{Binding CheckboxSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                IsChecked="{Binding ModIntSkill.IsTagged, RelativeSource={RelativeSource AncestorType=UserControl}}">

            </CheckBox>

            <local:TextBoxWithLabelControl
                x:Name="LabeledTextBox"
                FlowDirection="{Binding FlowDirection, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Orientation="{Binding Orientation, RelativeSource={RelativeSource AncestorType=UserControl}}"
                HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"
                HorizontalContentAlignment="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
                MaxWidth="{Binding MaxWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                MinWidth="{Binding MinWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                
                ModValue="{Binding ModIntSkill, RelativeSource={RelativeSource AncestorType=UserControl}}"
                IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                CanOpenModal="{Binding CanOpenModal, RelativeSource={RelativeSource AncestorType=UserControl}}"
                LabelText="{Binding LabelText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                Hint="{Binding Hint, RelativeSource={RelativeSource AncestorType=UserControl}}"
                />

            <ItemsControl ItemsSource="{Binding ModIntSkill.SkillModifiers, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <RadioButton
                            Style="{StaticResource FalloutSkillRadioButton}"
                            VerticalAlignment="Center"
                            Content="{Binding Converter={StaticResource SPECIALToShortConverter}}"
                            GroupName="{Binding ModIntSkill.Name, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            FontFamily="{StaticResource JH_Fallout}"
                            FontSize="7"
                            Checked="RadioButton_Checked">
                            <RadioButton.IsChecked>
                                <MultiBinding Converter="{StaticResource EqualityToBoolConverter}" Mode="TwoWay">
                                    <Binding Path="."/>
                                    <Binding Path="ModIntSkill.SelectedModifier" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                </MultiBinding>
                            </RadioButton.IsChecked>
                        </RadioButton>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
        </StackPanel>
    </Grid>
</UserControl>
