    <UserControl x:Class="WPF_Fallout_Character_Manager.Controls.TextBoxWithLabelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Fallout_Character_Manager.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel
            FlowDirection="{Binding FlowDirection, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Orientation="{Binding Orientation, RelativeSource={RelativeSource AncestorType=UserControl}}"
            HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <Label
                Style="{StaticResource FalloutDefaultLabel}"
                FontSize="{Binding LabelFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"
                VerticalAlignment="Center"
                Content="{Binding LabelText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                />
            <local:EnhancedTextBox x:Name="EnhancedTextBox"
                HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"
                HorizontalContentAlignment="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}"
                MaxWidth="{Binding MaxWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                MinWidth="{Binding MinWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Padding="{Binding TextPadding, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                TextWrapping="{Binding TextWrapping, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FlowDirection="LeftToRight"
                VerticalAlignment="Center"
                ModInt="{Binding ModInt, RelativeSource={RelativeSource AncestorType=UserControl}}"
                IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                CanOpenModal="{Binding CanOpenModal, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                PreviewMouseRightButtonDown="EnhancedTextBox_PreviewMouseRightButtonDown">
                <local:EnhancedTextBox.InputBindings>
                    <MouseBinding Gesture="RightClick"
                        Command="{Binding OpenModifierModalWindowCommand}"
                        CommandParameter="{Binding ModInt, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                </local:EnhancedTextBox.InputBindings>
            </local:EnhancedTextBox>
        </StackPanel>
    </Grid>
</UserControl>
