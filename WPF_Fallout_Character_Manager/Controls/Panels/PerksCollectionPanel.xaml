<UserControl x:Class="WPF_Fallout_Character_Manager.Controls.Panels.PerksCollectionPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Fallout_Character_Manager.Controls.Panels"
             xmlns:control="clr-namespace:WPF_Fallout_Character_Manager.Controls"
            xmlns:conv="clr-namespace:WPF_Fallout_Character_Manager.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="760" d:DesignWidth="1015">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox
            Header="Traits Collection"
            Grid.Row="0"
            Margin="5"
            Background="Black"
            BorderBrush="#23ba03"
            BorderThickness="1"
            Padding="5">
            <control:TPCardControl
                Width="250"
                Height="250"
                HorizontalAlignment="Left"
                DataContext="{Binding SelectedCatalogueTrait}"
                WildWastelandVisibility="Visible"/>
        </GroupBox>

        <GroupBox
            Header="Perks Collection"
            Grid.Row="1"
            Margin="5"
            Background="Black"
            BorderBrush="#23ba03"
            BorderThickness="1"
            Padding="5">
            <control:TPCardControl
                Width="250"
                Height="250"
                VerticalAlignment="Top"
                HorizontalAlignment="Left"
                DataContext="{Binding SelectedCataloguePerk}"
                WildWastelandVisibility="Collapsed"/>
        </GroupBox>
    </Grid>
</UserControl>
