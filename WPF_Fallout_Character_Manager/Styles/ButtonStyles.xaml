<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="FalloutTopBarButton" TargetType="Button">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontFamily" Value="{StaticResource Monofonto}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#23ba03"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="18"/>
        <Setter Property="Height" Value="18"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter VerticalAlignment="Center"
                                      HorizontalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="FalloutMinimizeButtonStyle" BasedOn="{StaticResource FalloutTopBarButton}" TargetType="Button">
        <Setter Property="Content" Value=" - "/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Content" Value="[-]"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="FalloutFullscreenButtonStyle" BasedOn="{StaticResource FalloutTopBarButton}" TargetType="Button">
        <Setter Property="Content" Value=" o "/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Content" Value="[o]"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="FalloutCloseButtonStyle" BasedOn="{StaticResource FalloutTopBarButton}" TargetType="Button">
        <Setter Property="Content" Value=" x "/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Content" Value="[x]"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>